name: example
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_based_router_annotations:
    path: ../file_based_router_annotations

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.4
  test: ^1.24.1
  file_based_router_generator:
    path: ../file_based_router_generator

flutter:
  uses-material-design: true
